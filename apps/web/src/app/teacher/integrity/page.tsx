import { IntegrityCheckPage } from "@/features/integrity/integrity-check";

export const metadata = { title: "Integrity Check — Ed8AI" };

export default function TeacherIntegrityPage() {
  return <IntegrityCheckPage />;
}
