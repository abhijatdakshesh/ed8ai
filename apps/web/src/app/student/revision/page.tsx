import { RevisionPlanPage } from "@/features/revision/revision-plan";

export const metadata = { title: "My Revision Plan — Ed8AI" };

export default function StudentRevisionPage() {
  return <RevisionPlanPage />;
}
